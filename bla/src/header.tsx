//src/components/header.tsx
import NextLink from "next/link"
import { Button } from '@material-ui/core'

const siteTitle="Sauce"
export default function Header() {

  return (
   <div>
      
      <Button >Button for Account (wat)</Button>
    </div>
  )
}